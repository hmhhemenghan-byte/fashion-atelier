# NÉRA ATELIER Project Rules

本文件约束仓库内所有人工与 AI 协作开发。适用范围为整个项目；子目录如有更具体的 `AGENTS.md`，以距离目标文件最近的规则为补充。

## 1. 产品与品牌

- 正式品牌名为 **NÉRA ATELIER**，中文名为 **霓岚时装设计馆**。
- 对外定位为当代时装设计展示、专业发布与设计生产档案平台，不把产品改造成通用电商。
- 未经明确需求，不新增价格、购物车、下单、支付或自动营销功能。
- `fashion-atelier` 是仓库与部署标识；需要纯 ASCII 的文件名或机器标识时使用 `NERA`。
- 已有业务术语、阶段编号、数据口径和人工审批边界必须保持一致。

## 2. 设计规则

- 视觉方向：高端、克制、编辑化、具有时装发布与档案馆质感。
- 优先使用强排版、充分留白、清晰层级和高质量作品图，不堆叠无意义装饰。
- 新页面必须同时适配桌面端和移动端；不得只针对单一截图尺寸实现。
- 交互必须支持键盘操作、清晰焦点状态、语义化 HTML 和可理解的无障碍标签。
- 图片必须维护准确的 `alt` 文本；装饰图片使用空 `alt`，不能用文件名代替描述。
- 不随意引入新的字体、颜色和组件风格；优先复用 `app/globals.css`、现有页面样式和组件语言。
- 不加入第三方品牌 Logo、水印或来源不明的视觉素材。

## 3. 技术边界

- 当前运行架构为 Vinext/Vite + React + Cloudflare Workers。
- D1 绑定名固定为 `DB`，R2 绑定名固定为 `BUCKET`；不得擅自修改 `.openai/hosting.json` 中的 `project_id` 或绑定名。
- 服务端数据访问集中在 `db/` 与 `lib/`，路由层负责鉴权、输入解析和响应，不复制业务规则。
- 页面与组件放在 `app/`；共享业务逻辑放在 `lib/`；数据库定义放在 `db/schema.ts`。
- 不将 D1、R2 或服务端密钥暴露给浏览器端代码。
- 不把真实生产数据、上传图片二进制、密钥或个人信息提交进 Git。
- 新依赖必须有明确用途；优先使用现有依赖和 Web 平台能力。
- 依赖版本必须通过 `package-lock.json` 锁定，禁止仅修改 `package.json`。

## 4. 数据库与迁移

- 数据结构变更必须先修改 `db/schema.ts`，再运行 `npm run db:generate` 生成新的 Drizzle 迁移。
- 已提交的历史迁移不可重写、改名或删除；修复必须新增迁移。
- 迁移必须兼容已有数据，明确默认值、空值策略、索引和唯一性约束。
- 破坏性迁移必须先提供备份、回填和回滚方案，不允许隐式丢弃字段或记录。
- 统计、准备度和状态结论必须来自明确事实，不新增隐藏评分或不可解释权重。

## 5. 上传与媒体

- 上传入口必须进行设计师身份校验，并同时校验文件类型、大小和数量。
- R2 只存储对象；D1 保存稳定对象键、业务关系、描述、排序和状态。
- 替换媒体时不得静默破坏已有作品链接或业务关联。
- 私密试身、封样和生产证据默认保持私密，不因相关记录获批而自动公开。
- 所有新增媒体类型必须进入 Archive & Handoff 的媒体清单与迁移策略。
- 不把大型图片二进制写入 JSON、数据库字段或 Git 仓库。

## 6. 身份、安全与隐私

- 管理员通过运行环境中的 `ADMIN_EMAILS` 配置；仓库只保留 `.env.example`。
- 所有写操作必须在服务端再次鉴权，不能只依赖界面隐藏按钮。
- API 必须验证输入、拒绝未知或越权字段，并返回可诊断但不泄露内部信息的错误。
- Private Showroom 只保存访问凭证哈希；不得记录或返回明文凭证。
- 日志不得输出访问令牌、邮箱列表、完整凭证、私密图片地址或请求正文中的敏感信息。
- 系统只生成和记录外联草稿；除非用户明确接入并批准发送能力，不得自动发送邮件、消息或邀约。

## 7. 状态与审批

- 设计评审、技术包批准、试身批准、封样、生产放行和生产偏差决定必须由设计师明确操作。
- 系统不得自动批准、自动锁定、伪造已发送状态或把建议当成已发生事实。
- 已冻结、已签核或已形成正式决定的记录不得原地覆盖；后续变化通过新修订、新轮次或时间线记录表达。
- 所有重要状态变化应保留时间、操作者、依据及可追溯关系。

## 8. 代码质量

- TypeScript 代码保持类型明确，避免无理由使用 `any`、非空断言和宽泛类型转换。
- 复用已有输入解析、运行时、鉴权和业务模块，不在多个 API 路由重复实现同一规则。
- 新增 API 时同时处理成功、无权限、无效输入、不存在和运行时故障场景。
- 不吞掉异常；面向用户的错误简洁，服务端诊断信息不包含敏感数据。
- 复杂业务判断应拆成可测试的纯函数，并使用具有业务含义的命名。
- 删除已经确认无用的代码，但不得顺手重构与当前任务无关的大范围模块。

## 9. 必须通过的检查

提交前至少运行：

```bash
npm run lint
npm test
```

`npm test` 会执行生产构建、Sites 产物验证和渲染结果测试。若只检查部署产物，可运行：

```bash
npm run build
npm run validate:artifact
```

数据库结构变化还必须检查新迁移、Drizzle 元数据以及旧数据兼容性。

## 10. Git 与交付

- 从最新 `main` 创建短生命周期分支，建议格式：`feature/...`、`fix/...`、`docs/...`。
- 一个提交只表达一个可回滚目的；提交信息说明结果，不写“update”之类模糊描述。
- Pull Request 必须说明变更范围、风险、验证结果、数据迁移和界面截图（如涉及视觉）。
- 不提交 `node_modules/`、`dist/`、`.next/`、`.sites-runtime/`、`.env` 或线上数据导出。
- 发布前确认源码提交、Sites 版本和构建产物来自同一个 commit。
- D1 数据与 R2 媒体不属于源码包；正式迁移或灾备时必须单独导出。

## 11. 完成定义

只有同时满足以下条件，任务才算完成：

- 功能满足需求且没有越过产品、权限和人工审批边界。
- 桌面端、移动端、键盘操作和空数据/错误状态可用。
- 数据结构、迁移、归档和媒体清单保持一致。
- Lint、生产构建、产物验证和测试全部通过。
- 相关 README、阶段文档或工作流文档已更新。
- 没有提交密钥、个人数据、临时文件或运行产物。

