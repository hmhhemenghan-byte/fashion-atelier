# Comma-separated authenticated account emails allowed to manage作品.
ADMIN_EMAILS=you@example.com

# ChatGPT Sites keeps AUTH_PROVIDER unset and supplies its own identity headers.
# Direct Cloudflare deployment reads these values from wrangler.cloudflare.json.
# AUTH_PROVIDER=cloudflare-access
# TEAM_DOMAIN=https://your-team.cloudflareaccess.com
# POLICY_AUD=your-access-application-audience-tag
